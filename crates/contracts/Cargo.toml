[package]
name = "prometheus-contracts"
version = "0.0.1"
edition = "2021"
license = "MIT"
description = "Versioned schemas for every Prometheus module boundary"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
