[package]
name = "prometheus-ledger"
version = "0.0.1"
edition = "2021"
license = "MIT"
description = "Append-only experiment ledger with SQL and embedding search"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
